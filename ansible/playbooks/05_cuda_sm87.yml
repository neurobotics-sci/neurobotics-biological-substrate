---
- name: "Bubo v50.0 — 05_cuda_sm87"
  hosts: jetson_orin
  become: true
  tasks:
    - include_role:
        name: cuda_sm87
  handlers:
    - name: reload systemd
      systemd: daemon_reload=yes
