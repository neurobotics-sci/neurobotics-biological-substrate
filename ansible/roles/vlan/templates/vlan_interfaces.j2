# VLAN sub-interfaces — {{ inventory_hostname }}
# Generated by Ansible — Bubo v5400

{% if node_vlan_10 | default(false) %}
auto eth0.10
iface eth0.10 inet static
  address {{ node_ip_vlan10 }}
  netmask 255.255.255.0
  vlan-raw-device eth0
{% endif %}

{% if node_vlan_20 | default(false) %}
auto eth0.20
iface eth0.20 inet static
  address {{ node_ip_vlan20 }}
  netmask 255.255.255.0
  vlan-raw-device eth0
{% endif %}

{% if node_vlan_30 | default(false) %}
auto eth0.30
iface eth0.30 inet static
  address {{ node_ip_vlan30 }}
  netmask 255.255.255.0
  vlan-raw-device eth0
{% endif %}
